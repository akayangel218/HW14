#include <iostream>
#include <vector>

using namespace std;

vector<int> compare(vector<int> h, int start, int end) {
    //if 
}

vector<int> minmax(vector<int> n, int start, int end) {
    if (n.size() == 1) {
        return n;
    }
    else {
        //compare(n)
        //compare(n)
    }
}

int main() {

    vector<int> test;
    return 0;
}